# Allow all crawlers
User-agent: *
Allow: /

# Disallow specific paths (if any)
Disallow: /api/
Disallow: /admin/

# Sitemap location
Sitemap: https://lcrremodeling.com/sitemap.xml

# Crawl delay (optional - helps prevent server overload)
Crawl-delay: 1
